# Lootsy Mobile (Expo) – 3.1
- Använder redirect-API för klickloggning.
- Ändra `extra.apiBaseUrl` i `app.json` till din produktionswebb.
- `npm i && npm start`
- Bygg för Google Play med EAS när webben är live.
